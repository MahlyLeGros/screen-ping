# Docs

| File | Purpose |
|------|---------|
| `FRIEND-SETUP.txt` | Short install guide shipped with the friend share zip |

Generated friend packages land in `../out/` via `scripts/package-for-friend.bat`.
