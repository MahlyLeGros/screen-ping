{
  "name": "Screen Ping",
  "install": "bash .cursor/install.sh",
  "ports": [
    { "name": "web", "port": 5173 },
    { "name": "api", "port": 8000 }
  ],
  "terminals": [
    {
      "name": "server",
      "command": "cd server && . .venv/bin/activate && SERVE_SPA=false uvicorn run:app --reload --host 0.0.0.0 --port 8000",
      "description": "FastAPI + Socket.IO backend on http://localhost:8000 (SQLite, API only)."
    },
    {
      "name": "web",
      "command": "cd web && npm run dev -- --host 0.0.0.0 --port 5173",
      "description": "React + Vite dashboard on http://localhost:5173 (proxies /api, /uploads, /socket.io to :8000)."
    }
  ]
}
